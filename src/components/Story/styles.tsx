import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    borderColor: 'red',
  },
  name: {
    textAlign: 'center',
    paddingBottom: 10,
  },
});

export default styles;

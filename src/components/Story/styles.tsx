import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    borderColor: 'red',
  },
  name: {
    height: 20,
    textAlign: 'center',
    paddingBottom: 10,
    maxWidth: 70,
    overflow: 'hidden',
  },
});

export default styles;
